# Velo v0.9.1

本次预览版优化启动台界面、主题显示与搜索交互。

- 更新首页、搜索结果及设置面板样式，统一使用本地打包的 Microsoft Fluent UI System Icons，并提供开源许可说明。
- 改善浅色、深色和跟随系统主题的背景同步，减少启动与再次呼出时的背景闪烁；没有固定应用时不再显示空的固定分组。
- 点击主页面内其他区域时，搜索框保持焦点；返回主页面后自动恢复输入焦点。
- 搜索框有内容时，按 Esc 清空输入并回到主页；输入为空时按 Esc 隐藏窗口。底部提示随输入状态切换。
- 补充系统快捷入口的英文名称、拼音和缩写，例如 calculator、rwglq、hjbl；入口无需固定即可检索，取消固定后仍可搜索。

## 下载

Windows x64，需要 Microsoft Edge WebView2 Runtime。

| 文件 | 大小 | 说明 |
| --- | --- | --- |
| `velo-launcher.exe` | 12000768 字节 | 独立程序，放到固定目录后直接运行 |
| `velo-launcher-amd64-installer.exe` | 6718648 字节 | NSIS 安装包，默认安装到 `%ProgramFiles%\HBLADEH\Velo` |
| `SHA256SUMS.txt` | 186 字节 | 上述文件的 SHA-256 校验值，供手动校验与应用内自动更新使用 |

## 校验值

```text
d8a2175618f206740e00950d076d898bb19e829026d78ed00737f6b33fe939fb  velo-launcher.exe
ef7bb2800b8f9bbdc09eb8963a5d99bb9ffcce1030518faaa5d89f2faedd9dcc  velo-launcher-amd64-installer.exe
```

```powershell
Get-FileHash .\velo-launcher.exe -Algorithm SHA256
```

> 发布文件未签名。Windows SmartScreen 可能提示未知发布者，请先核对上面的 SHA-256 再运行。

原生窗口焦点、主题切换及安装后的完整自动更新流程仍需人工回归。
